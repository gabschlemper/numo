<script setup lang="ts">
// Root route redirects straight to the one screen this prototype
// implements. Kept as a real page (not a nuxt.config redirect) so the
// intent is visible in the file tree.
await navigateTo('/transactions')
</script>

<template>
  <div />
</template>
